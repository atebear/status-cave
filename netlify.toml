[build]
  publish = "public"
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.125.0"

[[headers]]
  for = "/*.json"
  [headers.values]
    Access-Control-Allow-Origin = "*" # Allows the site to fetch status JSON without CORS errors
